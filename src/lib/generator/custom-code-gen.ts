/**
 * Returns empty custom code for MVP.
 * In the future, this could generate Google Analytics snippets,
 * custom CSS, or other code based on blueprint settings.
 */
export function generateCustomCode(): string {
  return JSON.stringify([]);
}
